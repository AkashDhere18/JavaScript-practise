let promise = new Promise((resolve, reject) => {

    let success = false;
    if (success) {
        resolve(console.log("Data fetched successfully!"));
    } else {
        reject(console.log("Error while fetching data."));
    }
});